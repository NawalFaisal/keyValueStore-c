# keyValueStore-c

A lightweight in-memory key-value store written in C.

## Features
- SET: store a key-value pair
- GET: retrieve a value by key
- DELETE: remove a key-value pair
- Dynamic linked list with heap memory management

## Build
gcc keyValue.c -o keyValue && ./keyValue